{# 퍼블리싱 가이드 메뉴리스트 #}
<header class="header">
    <nav class="is-mobile">
        <a href="#" class="icon-menu">메뉴보기</a>
    </nav>
</header><!-- // .header -->
<aside class="aside">
    <div class="aside-menu">
        <nav class="navigation">
            <div class="main-nav">
                <ul class="main-menu">
                    <li>
                        <a href="#">퍼블리싱 리스트</a>
                        <ul class="dep1">
                            {% for title in publist_data %}
                            <li><a href="index.html#{{title.href}}">{{title.nav}} ({{title.end}})</a></li>
                            {%- endfor %}
                        </ul>
                    </li>
                    <li>
                        <a href="#">가이드</a>
                        <ul class="dep1">
                            {% for title in pubguide_data %}
                            <li><a href="{{title.href}}">{{title.name}}</a></li>
                            {%- endfor %}
                        </ul>
                    </li>
                </ul>
            </div>
        </nav>
    </div>
</aside>