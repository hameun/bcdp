{%- extends "pubguide/_layout.njk" -%}
{%- import "_templates/_macro/_macro.njk" as macro -%}

{% block content %}
<h1 class="title1">라이브러리/etc</h1>

<h2 class="title2">데이트피커
    <p>링크 : <a href="https://air-datepicker.com/" target="_blank" class="button-xsmall">AIR DATEPICKER</a></p>
</h2>

<div class="preview">
    <div class="input-box">
        <input type="text" class="ipt" id="date1" placeholder="날짜 입력">
    </div>
    <script>
        let startDate = new Date('2025-07-25');
        new AirDatepicker('#date1', {
            firstDay: 0,
            startDate,
            range: true,
            navTitles: {days: '<i>yyyy</i>.MMMM'},
            selectedDates: [startDate],
            multipleDatesSeparator: '-',
            position({$datepicker, $target, $pointer}) {
                let coords = $target.getBoundingClientRect()
                let top = window.scrollY + coords.height + coords.y + 10;
                $datepicker.style.top = `${top}px`;
                $datepicker.style.left = `${coords.x -2}px`;
                $pointer.style.display = 'none';
            }
        });
    </script>
</div>

<h2 class="title2">
    셀렉트박스 레이어메뉴
    <p>공통으로 사용하고 있으나 필요시 각 페이지에 삽입 부탁드립니다. </p>
    <p>링크 : <a href="https://jqueryui.com/selectmenu/" target="_blank" class="button-xsmall">jQuery UI > selectmenu</a></p>
</h2>
{# <button type="button">ajax 호출</button> #}

<div id="ajaxTest">
    <div class="preview">
        <div class="contents-summary">
            <div class="page-select">
                <select>
                    <option value="10">최신순</option>
                    <option value="10">조회순</option>
                </select>
            </div>
        </div>
    </div>

</div>

<div class="codeview">
<pre>
fn_selectbox();
$("셀렉트박스").selectmenu({
    width: 160,
    position: {
        of: select.parentElement,
        at: "right bottom",
        my: "right top+8"
    },
    open: function( event ) {
        $(event.target.id).toggleClass('open');
        const menu = $('#' + event.target.id + '-menu')[0];
        if ( $(menu.children).hasClass('selected') ) return false;
        $(menu.firstChild).addClass('selected');
    },
    change: function( event ) {
        $(event.currentTarget).siblings().removeClass('selected');
        $(event.currentTarget).addClass('selected');
    }
});
</pre>
</div>

<h2 class="title2">슬라이더
    <p>링크 : <a href="https://swiperjs.com/" target="_blank" class="button-xsmall">Swiper</a></p>
</h2>
<h3>미리보기 퍼블리싱 페이지 확인</h3>
<div class="preview">
    <a href="../BA01.html" target="_blank" class="button-xsmall">상권분석</a>
    <a href="../AI01.html" target="_blank" class="button-xsmall">AI인사이트 리스트</a>
    <a href="../DM02.html" target="_blank" class="button-xsmall">데이터마켓 상세</a>
</div>
{%- endblock %}